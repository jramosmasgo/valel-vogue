// Barrel export — import everything auth-related from this single path:
//   import { loginWithEmail, onAuthChange, ... } from '@/services/authentication';

export * from './auth.service';
export * from './auth.types';
